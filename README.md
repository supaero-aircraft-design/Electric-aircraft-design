# Introduction to Electrified Aircraft Propulsion (EAP) design

This course aims to provide an introduction to the design of aircraft with electrified propulsive architecture. It also aims to highlight a few of the main design drivers and compare various solution for the electrification of propulsive function.

## 🚀 Installation

This course requires Python 3.10 or 3.11. 

### Local installation
To install the course on your personal laptop or on a school computer, first create a directory where all the files will be located. Then, download the wheel file (fast_oad_cs23_he-0.0.1-py3-none-any.whl) from the LMS and put it in the current directory. 
Open a Anaconda Prompt terminal and `cd` to the recently downloaded file.
You can then create a new conda environment by running:

```{.bash}
conda create -n electric-aircraft-design python=3.11
conda activate electric-aircraft-design
```

Then, install the required dependencies:
```{.bash}
pip install fast_oad_cs23_he-0.0.1-py3-none-any.whl
```

You can now use jupyter lab to proceed with the seminar by running `jupyter lab` in your command prompt.

--------


*Written by Florent Lutz (ISAE-SUPAERO), Toulouse, France.*